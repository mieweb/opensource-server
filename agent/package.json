{
  "name": "opensource-agent",
  "version": "0.0.0",
  "private": true,
  "description": "MIE opensource-server site agent: checks in with the manager, reports host/service status, and renders and applies nginx and dnsmasq configuration.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@particle/dbus-next": "^0.11.4",
    "ejs": "^3.1.10"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
