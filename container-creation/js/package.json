{
  "name": "container-creation-js",
  "version": "1.0.0",
  "description": "Authentication modules for container creation",
  "main": "runner.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "qs": "^6.12.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "axios-mock-adapter": "^1.22.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "*.js",
      "!runner.js",
      "!coverage/**"
    ]
  }
}